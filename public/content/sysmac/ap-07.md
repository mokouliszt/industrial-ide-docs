---
vendor: sysmac
vendor_label: "Sysmac Studio (オムロン)"
id: "ap-07"
num: "A-7"
title: "シミュレーション命令"
category: reference
source: "Sysmac Studio Version 1 オペレーションマニュアル (SBCA-470) pp.909-928"
---

# A-7 シミュレーション命令

- モーション制御命令をシミュレーションする場合に、原点近傍入力信号や限界入力信号などの外部信号を擬似的に発生させる必要があります。シミュレーション命令は、その外部信号を擬似的に発生させるための命令です。

- 正方向限界入力信号命令

- 負方向限界入力信号命令

- 原点近傍信号命令

- 即停止入力信号命令

- 外部ラッチ入力信号命令

- ドライバアラーム発生命令

- ドライバワーニング発生命令

- エンコーダ速度入力命令

- フィードバック位置書込命令

- フィードバック速度書込命令

- フィードバックトルク書込命令使用上の注意シミュレーション命令は、イベントタスク内では使用できません。

## A-7-1 正方向限界入力信号命令

擬似的に正方向限界入力信号を発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SigPosLmt _ | 正方向 限界入力信 号 | FB | SIM SigPosLmt instance _ _ SIM SigPosLmt _ Axis Axis Enable Status Busy Error ErrorID | SIM SigPosLmt instance ( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると正方向限界入力信号を「ON」 します。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Enable（有効）をTRUE にすると、Axis（軸）で指定した軸の正方向限界入力信号が擬似的に発生します。

- Enable（有効）をFALSE にすると、Axis（軸）で指定した軸の正方向限界入力信号が「OFF」となります。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-2 負方向限界入力信号命令

擬似的に負方向限界入力信号を発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SigNegLmt _ | 負方向 限界入力信 号 | FB | SIM SigNegLmt instance _ _ SIM SigNegLmt _ Axis Axis Enable Status Busy Error ErrorID | SIM SigNegLmt instance ( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると負方向限界入力信号を「ON」 します。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Enable（有効）をTRUE にすると、Axis（軸）で指定した軸の負方向限界入力信号が擬似的に発生します。

- Enable（有効）をFALSE にすると、Axis（軸）で指定した軸の負方向限界入力信号が「OFF」となります。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-3 原点近傍信号命令

擬似的に原点近傍信号を発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SigOrgPro _ x | 原点近傍信 号 | FB | SIM SigOrgProx instance _ _ SIM SigOrgProx _ Axis Axis Enable Status Busy Error ErrorID | SIM SigOrgProx instance ( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると原点近傍信号を「ON」しま す。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Enable（有効）をTRUE にすると、Axis（軸）で指定した軸の原点近傍信号が擬似的に発生します。

- Enable（有効）をFALSE にすると、Axis（軸）で指定した軸の原点近傍信号が「OFF」となります。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-4 即停止入力信号命令

擬似的に即停止入力信号を発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SigEmergS _ top | 即停止入力 信号 | FB | SIM SigEmergStop instance _ _ SIM SigEmergStop _ Axis Axis Enable Status Busy Error ErrorID | SIM SigEmergStop instance _ _ ( Axis :=《パラメータ》, Enable :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると即停止入力信号を「ON」し ます。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Enable（有効）をTRUE にすると、Axis（軸）で指定した軸の即停止入力信号が擬似的に発生します。

- Enable（有効）をFALSE にすると、Axis（軸）で指定した軸の即停止入力信号が「OFF」となります。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-5 外部ラッチ入力信号命令

擬似的にラッチ用の外部入力信号を発生させます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SigExtLatc _ h | 外部ラッチ 入力信号 | FB | SIM SigExtLatch instance _ _ SIM SigExtLatch _ Axis Axis Enable Status LatchID Busy Error ErrorID | SIM SigExtLatch instance ( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, LatchID :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると外部ラッチ入力信号を「ON」 します。 |

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| LatchID*1 | ラッチID 選択 | UINT | 1, 2 | 1 | 2 つのラッチ機能のうちのどちらを使用す るかを指定します 1：ラッチ機能1 2：ラッチ機能2 |

*1. LatchID については、『NJ/NX シリーズ CPU ユニット ユーザーズマニュアル モーション制御編（SBCE-433）』または『NY シリーズ 産業用パネル型PC ／産業用ボックス型PC ユーザーズマニュアル モーション制御編（SBCE-379）』を参照してください。

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Enable（有効）をTRUE にすると、Axis（軸）で指定した軸の外部入力信号が擬似的に発生します。

- Enable（有効）をFALSE にすると、Axis（軸）で指定した軸の外部入力信号が「OFF」となります。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

- 以下の場合に異常となります。

- LatchID（ラッチID 選択）が「1」または「2」以外の場合、エラーコードとして［0400 Hex］が出力されます。

- Axis（軸）に該当するスレーブがない場合、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-6 ドライバアラーム発生命令

擬似的にドライバアラームを発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetDrvAlm _ | ドライバ アラーム発 生 | FB | SIM SetDrvAlm instance _ _ SIM SetDrvAlm _ Axis Axis Execute Done Busy Failure Error ErrorID | SIM SetDrvAlm instance ( _ _ Axis :=《パラメータ》, Execute :=《パラメータ》, Done =>《パラメータ》, Busy =>《パラメータ》, Failure =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Execute | 起動 | BOOL | TRUE, FALSE | FALSE | 立ち上がり時に命令を開始します。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Done | 完了 | BOOL | TRUE, FALSE | 命令の実行が完了したときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Failure | 不正終了 | BOOL | TRUE, FALSE | 命令が正常に実行されなかったときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Execute（起動）の立ち上がりで、Axis（軸）で指定した軸のドライバアラームが擬似的に発生します。

- ドライバアラームの解除はMC_Reset（軸エラーリセット）命令で解除します。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-7 ドライバワーニング発生命令

疑似的にドライバワーニングを発生させます。サーボ軸設定時のみ使用できます。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetDrvWar _ n | ドライバ ワーニング 発生 | FB | SIM SetDrvWarn instance _ _ SIM SetDrvWarn _ Axis Axis Execute Done Busy Failure Error ErrorID | SIM SetDrvWarn instance ( _ _ Axis :=《パラメータ》, Execute :=《パラメータ》, Done =>《パラメータ》, Busy =>《パラメータ》, Failure =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Execute | 起動 | BOOL | TRUE, FALSE | FALSE | 立ち上がり時に命令を開始します。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Done | 完了 | BOOL | TRUE, FALSE | 命令の実行が完了したときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Failure | 不正終了 | BOOL | TRUE, FALSE | 命令が正常に実行されなかったときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| | 軸 | sAXIS REF _ _ | － | |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

- Execute（起動）の立ち上がりで、Axis（軸）で指定した軸のドライバワーニングが擬似的に発生します。

- ドライバワーニングは、擬似発生した次のサイクルで解除されます。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似入力信号を発生させるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-8 エンコーダ速度入力命令

エンコーダ軸に対して擬似的に現在速度を書き込みます。エンコーダ軸設定時のみ使用できます。本命令は、CPU ユニットVer.1.02 以降で使用可能です。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetVelocity _ | エンコーダ 速度入力 | FB | SIM SetVelocity instance _ _ SIM SetVelocity _ Axis Axis Execute Done Velocity Busy Failure Error ErrorID | SIM SetVelocity instance( _ _ Axis :=《パラメータ》, Execute :=《パラメータ》, Velocity :=《パラメータ》, Done =>《パラメータ》, Busy =>《パラメータ》, Failure =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| | 起動 | BOOL | TRUE, FALSE | FALSE | |

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Velocity | 現在速度 | LREAL | 正数、「0」、 負数 | 0 | 現在速度を指定します。（単位は[pulse/s]） |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Done | 完了 | BOOL | TRUE, FALSE | 命令の実行が完了したときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令が受け付けられたときにTRUE、命令処理完了時に FALSE となります。 |
| Failure | 不正終了 | BOOL | TRUE, FALSE | 命令が正常に実行されなかったときにTRUE となります。 * Sysmac Studio Ver.1.03 では常にFALSE となりま す。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

「Execute（起動）」の立ち上がりで、Axis（軸）で指定した軸の現在速度を擬似的に変更します。

使用上の注意

本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、疑似的に現在速度を変更するため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

異常

Axis（軸）に該当するスレーブがない場合、異常となり、エラーコードとして［0406 Hex］が出力されます。エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-9 フィードバック位置書込命令

ドライバエミュレータから、軸変数のプロセスデータオブジェクトに対してフィードバック位置を書き込みます。本命令は、CPU ユニットVer.1.09 以降で使用可能です。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetActPos _ | フィードバ ック位置書 込 | FB | SIM SetActPos instance _ _ SIM SetActPos _ Axis Axis Enable Status SetPos Busy Error ErrorID | SIM SetActPos instance( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, SetPos :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| Enable | 有効 | BOOL | TRUE, FALSE | FALSE | TRUE にすると命令を実行します。 |
| SetPos | フィードバッ ク位置 | LREAL | 正数、「0」、 負数 | 0 | フィードバック位置の絶対値を指定します。 （単位は［指令単位］） ロータリーモードの時は、近回り指定された ものとして動作します。 |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| | 軸 | sAXIS REF _ _ | － | |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

Enable（有効）がTRUE の間、Axis（軸）のプロセスデータオブジェクトに対して指定されたフィードバック位置を書き込みます。本命令が実行されている間は、ドライバエミュレータによるフィードバック位置の折り返し処理は行いません。Enable をFALSE にすると、命令実行前の動作モードを元に、フィードバック位置が更新されます。

使用上の注意

- 本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、擬似的にフィードバック位置のプロセスデータを書き替えるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

- 本命令はユーザプログラム中で一つの軸に対して1 個だけ使用してください。一つの軸に対して本命令を複数個使用した場合は、意図どおりに動作しない可能性があります。

- プロセスデータオブジェクトに書き込む値（電子ギアでパルスに変換した値）が32 ビットを超えないようにしてください。32 ビットを超えると意図どおりに動作しない可能性があります。

- 本命令を使用する場合は、軸ごとにインスタンスを割り当ててください。一つのインスタンスを複数の軸で使用することはできません。

- 本命令はエンコーダ軸には使用しないでください。意図どおりに動作しない可能性があります。

- 本命令はサーボドライブの代わりにプロセスデータオブジェクトのデータ更新を行う命令です。最も制御周期の早いタスクに割り当てて使用してください。

- 本命令には、以下の使用制限があります。a) 仮想軸に対する指令は使用できません。b) サーボOFF 状態では使用できません。c) 本命令を実行中にMC_Home 命令やMC_SetPosition 命令を実行することはできません。d) MC_WriteAxisParameter 命令によるギア比などの軸パラメータの変更内容は、本命令では無視されます。

異常

次の場合、異常となり、エラーコードが出力されます。

異常の原因エラーコード

Axis（軸）に該当するスレーブがない0406 Hex

フィードバック位置が入力可能な範囲を超えている0400 Hex

本命令に必要なオブジェクトが未設定3461 Hex

エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-10 フィードバック速度書込命令

ドライバエミュレータから、軸変数のプロセスデータオブジェクトに対してフィードバック速度を書き込みます。本命令は、CPU ユニットVer.1.09 以降で使用可能です。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetActVel _ | フィードバ ック速度書 込 | FB | SIM SetActVel instance _ _ SIM SetActVel _ Axis Axis Enable Status SetVel Busy Error ErrorID | SIM SetActVel instance( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, SetVel :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| | 有効 | BOOL | TRUE, FALSE | FALSE | |
| | フィードバッ ク速度 | LREAL | 正数、「0」、 負数 | 0 | |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Axis | 軸 | sAXIS REF _ _ | － | 軸を指定します。*1 |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

Enable（有効）がTRUE の間、Axis（軸）のプロセスデータオブジェクトに対して指定したフィードバック速度を書き込みます。Enable をFALSE にすると、フィードバック速度の書き込みを行いません。

使用上の注意

- 本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、擬似的にフィードバック速度のプロセスデータを書き替えるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

- 本命令を使用するには、プロセスデータオブジェクトのマッピングが必要です。マッピングされていない場合は実行時に異常となります。

- 本命令はユーザプログラム中で一つの軸に対して1 個だけ使用してください。一つの軸に対して本命令を複数個使用した場合は、意図どおりに動作しない可能性があります。

- プロセスデータオブジェクトに書き込む値（電子ギアでパルスに変換した値）が32 ビットを超えないようにしてください。32 ビットを超えると意図どおりに動作しない可能性があります。

- 本命令を使用する場合は、軸ごとにインスタンスを割り当ててください。一つのインスタンスを複数の軸で使用することはできません。

- 本命令はエンコーダ軸には使用しないでください。意図どおりに動作しない可能性があります。

- 本命令はサーボドライブの代わりにプロセスデータオブジェクトのデータ更新を行う命令です。最も制御周期の早いタスクに割り当てて使用してください。

- 本命令には、以下の使用制限があります。a) 仮想軸に対する指令は使用できません。b) サーボOFF 状態では使用できません。c) 本命令を実行中にMC_Home 命令やMC_SetPosition 命令を実行することはできません。d) MC_WriteAxisParameter 命令によるギア比などの軸パラメータの変更内容は、本命令では無視されます。

異常

次の場合、異常となり、エラーコードが出力されます。

| 異常の原因 | エラーコード |
|---|---|
| Axis（軸）に該当するスレーブがない | 0406 Hex |
| フィードバック速度が入力可能な範囲を超えている | 0400 Hex |
| 本命令に必要なオブジェクトが未設定 | 3461 Hex |

エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-11 フィードバックトルク書込命令

ドライバエミュレータから、軸変数のプロセスデータオブジェクトに対してフィードバックトルクを書き込みます。本命令は、CPU ユニットVer.1.09 以降で使用可能です。

| 命令 | 名称 | FB/ FUN | グラフィック表現 | ST 表現 |
|---|---|---|---|---|
| SIM SetActTrq _ | フィードバ ックトルク 書込 | FB | SIM SetActTrq instance _ _ SIM SetActTrq _ Axis Axis Enable Status SetTrq Busy Error ErrorID > | SIM SetActTrq instance( _ _ Axis :=《パラメータ》, Enable :=《パラメータ》, SetTrq :=《パラメータ》, Status =>《パラメータ》, Busy =>《パラメータ》, Error =>《パラメータ》, ErrorID =>《パラメータ》 ); |

変数

- 入力変数

| 入力変数 | 名称 | データ型 | 有効範囲 | 初期値 | 内容 |
|---|---|---|---|---|---|
| | 有効 | BOOL | TRUE, FALSE | FALSE | |
| | フィードバッ クトルク | LREAL | 正数、「0」、 負数 | 0 | |

- 出力変数

| 出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| Status | 入力信号ON 中 | BOOL | TRUE, FALSE | 入力信号がON 状態になったときにTRUE となります。 |
| Busy | 実行中 | BOOL | TRUE, FALSE | 命令を受け付けたときにTRUE となります。 |
| Error | エラー | BOOL | TRUE, FALSE | 異常が発生したときにTRUE となります。 |
| ErrorID | エラーコード | WORD | － | 異常が発生したときに、エラーコードを出力します。 16#0000 は正常です。 |

- 入出力変数

| 入出力変数 | 名称 | データ型 | 有効範囲 | 内容 |
|---|---|---|---|---|
| | 軸 | sAXIS REF _ _ | － | |

*1. Sysmac Studio の軸基本設定画面で作成した軸変数（デフォルト「MC_Axis***」）、または軸変数のアドレスを指定したユーザ定義変数を使用してください。

機能説明

Enable（有効）がTRUE の間、Axis（軸）のプロセスデータオブジェクトに対して指定されたフィードバックトルクを書き込みます。サーボドライバへ出力する目標トルクを［0.1%］単位の比率で指定します。Enable をFALSE にすると、フィードバックトルクの書き込みを行いません。

使用上の注意

- 本命令はシミュレータ専用命令です。本命令は、通常のプログラムで使用しても実機では実行されませんが、擬似的にフィードバックトルクのプロセスデータを書き替えるため、デバッグプログラムで使用してください。デバッグプログラムは実機では実行されません。

- 本命令はユーザプログラム中で一つの軸に対して1 個だけ使用してください。一つの軸に対して本命令を複数個使用した場合は、意図どおりに動作しない可能性があります。

- プロセスデータオブジェクトに書き込む値（電子ギアでパルスに変換した値）が32 ビットを超えないようにしてください。32 ビットを超えると意図どおりに動作しない可能性があります。

- 本命令を使用する場合は、軸ごとにインスタンスを割り当ててください。一つのインスタンスを複数の軸で使用することはできません。

- 本命令はエンコーダ軸には使用しないでください。意図どおりに動作しない可能性があります。

- 本命令はサーボドライブの代わりにプロセスデータオブジェクトのデータ更新を行う命令です。最も制御周期の早いタスクに割り当てて使用してください。

- 本命令には、以下の使用制限があります。a) 仮想軸に対する指令は使用できません。b) サーボOFF 状態では使用できません。c) 本命令を実行中にMC_Home 命令やMC_SetPosition 命令を実行することはできません。d) MC_WriteAxisParameter 命令によるギア比などの軸パラメータの変更内容は、本命令では無視されます。

異常

次の場合、異常となり、エラーコードが出力されます。

異常の原因エラーコード

Axis（軸）に該当するスレーブがない0406 Hex

フィードバックトルクが入力可能な範囲を超えている0400 Hex

本命令に必要なオブジェクトが未設定3461 Hex

エラーコードの詳細については、後述の「A-7-12 エラーコードの詳細」を参照してください。

## A-7-12 エラーコードの詳細

命令で発生する異常の詳細情報を示します。異常は、そのエラーコードを下位4 けたにもつイベントコードで表されます。各エラーコードの説明は、対応するイベントコードの説明を参照してください。たとえば、命令のエラーコードが［0400Hex］の場合は、［54010400Hex］のイベントコードの説明を参照してください。

| イベント名 | 入力値範囲外 | | | イベントコード | 54010400Hex | |
|---|---|---|---|---|---|---|
| 内容 | 命令の入力パラメータが入力変数の範囲を超えています。 または、整数0 で除算／剰余演算を行いました | | | | | |
| 発生源 | PLC 機能モジュール | | 発生源詳細 | 命令 | 検出タイミン グ | 命令実行時 |
| 異常の属性 | 重要度 | 監視情報 | 復旧方法 | － | ログカテゴリ | システム |
| 発生の影響 | ユーザプログ ラム | 継続 | 動作 | 該当命令を仕様通りに終了 | | |
| システム定義 変数 | 変数名 | | データ型 | | 名称 | |
| | なし | | － | | － | |
| 発生要因とそ の処置 | 発生要因（推定原因） | | 処置対策 | | 再発防止 | |
| | 命令の入力パラメータが入力変数 の範囲を超えている。または、整 数0 で除算／剰余演算を行った | | 該当命令の入力変数の範囲を確認 し、入力パラメータが範囲を超え ないよう、または整数0 で除算／ 剰余演算しないようにしてくださ い | | 命令への入力パラメータの値が入 力範囲を超えないようにしてくだ さい | |
| 付属情報 | 付属情報1：異常の発生箇所 付属情報2：異常の発生箇所詳細 回路番号。プログラムセクションの場合はセクション先頭からの回路 番号。ST の場合は行番号 付属情報3：異常が発生した命令の命令名、命令インスタンス名。候補が複数ある場合は複数表示。特定 できない場合は表示なし 付属情報4：拡張エラーコード(ErrorIDEx) | | | | | |
| 注意点／備考 | 異常発生後にプログラムを変更した場合は、付属情報が正しく表示されない可能性があります | | | | | |

| イベント名 | 領域範囲外指定 | | | イベントコード | 54010406Hex | |
|---|---|---|---|---|---|---|
| 内容 | 命令で指定されたデータのメモリアドレスやデータサイズが不適切です | | | | | |
| 発生源 | PLC 機能モジュール | | 発生源詳細 | 命令 | 検出タイミン グ | 命令実行時 |
| 異常の属性 | 重要度 | 監視情報 | 復旧方法 | － | ログカテゴリ | システム |
| 発生の影響 | ユーザプログ ラム | 継続 | 動作 | 該当命令を仕様どおりに終了 | | |
| システム定義 変数 | 変数名 | | データ型 | | 名称 | |
| | なし | | － | | － | |
| 発生要因とそ の処置 | 発生要因（推定原因） | | 処置対策 | | 再発防止 | |
| | 命令で指定されたデータのメモリ アドレスが、有効範囲外である。 または、命令で指定されたデータ のサイズが、有効範囲を超えてい る。変数のデータ型とデータサイ ズとが合っていないことなどが考 えられる | | 該当命令で指定されるデータのメ モリアドレスやデータサイズを、 有効範囲内に収めるように修正し てください | | 命令で指定されるデータのメモリ アドレスやデータサイズが、有効 範囲内に収まるようにしてくださ い | |
| 付属情報 | 付属情報1：異常の発生箇所 付属情報2：異常の発生箇所詳細 回路番号。プログラムセクションの場合はセクション先頭からの回路 番号。ST の場合は行番号 付属情報3：異常が発生した命令の命令名、命令インスタンス名。候補が複数ある場合は複数表示。特定 できない場合は表示なし 付属情報4：拡張エラーコード(ErrorIDEx) | | | | | |
| 注意点／備考 | 異常発生後にプログラムを変更した場合は、付属情報が正しく表示されない可能性があります | | | | | |

| イベント名 | プロセスデータオブジェクト設定不足 | | | イベントコード | 54013461Hex | |
|---|---|---|---|---|---|---|
| 内容 | PDO マッピングが正しくありません | | | | | |
| 発生源 | PLC 機能モジュール | | 発生源詳細 | 命令 | 検出タイミン グ | 命令起動時 |
| 異常の属性 | 重要度 | 監視情報 | 復旧方法 | － | ログカテゴリ | システム |
| 発生の影響 | ユーザプログ ラム | 継続 | 動作 | 該当命令を仕様どおりに終了 | | |
| システム定義 変数 | 変数名 | | データ型 | | 名称 | |
| | MC AX[*].MFaultLvl.Active | | BOOL | | 軸 軽度フォールト発生中 | |
| 発生要因とそ の処置 | _ _ 発生要因（推定原因） | | 処置対策 | | 再発防止 | |
| | モーション命令で必要なPDO マ ッピングがされていない | | 該当命令で必要なPDO マッピン グを行ってください。 必要なPDO マッピングについて は、該当命令の「機能説明」を参 照してください | | 使用する命令で必要なPDO マッ ピングを行ってください。 各命令で必要なPDO マッピング （サーボドライバの設定）について は、『NJ/NX シリーズ ユーザーズ マニュアル モーション制御編』 または『NY シリーズ ユーザーズ マニュアル モーション制御編』を 参照してください | |
| | 該当命令に対応するオブジェクト を持たない対象機器に対して該当 命令を実行した | | 該当命令に対応する機能を持たな い対象機器があります。 対象機器のマニュアルを参照し て、該当命令に対応しているか確 認し、対応していない命令を実行 しないようにプログラムを修正し てください | | 対象機器のマニュアルを参照し、 対応していない命令を実行しない ようにプログラムを作成してくだ さい | |
| 付属情報 | 付属情報1：異常の発生箇所 付属情報2：異常の発生箇所詳細 回路番号。プログラムセクションの場合はセクション先頭からの回路 番号。ST の場合は行番号 付属情報3：異常が発生した命令の命令名、命令インスタンス名。候補が複数ある場合は複数表示。特定 できない場合は表示なし 付属情報4：拡張エラーコード(ErrorIDEx) | | | | | |
| 注意点／備考 | 異常発生後にプログラムを変更した場合は、付属情報が正しく表示されない可能性があります | | | | | |

