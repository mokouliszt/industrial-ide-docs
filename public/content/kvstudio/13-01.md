---
vendor: kvstudio
vendor_label: "KV STUDIO (キーエンス)"
id: "13-01"
num: "13-1"
title: "スクリプトの挿入"
category: programming
source: "KV STUDIO Ver.12 ユーザーズマニュアル (AS_151666) pp.488-490"
---

# 13-1 スクリプトの挿入

スクリプトの挿入方法について説明します。スクリプトの詳細については『KV-8000/7000/5000/3000/1000シリーズ・KV Nanoシリーズスクリプトプログラミングマニュアル』を参照してください。

ポイント

ST言語はCPU機能バージョン2.1以降のKV-8000(A)のみ使用できます。

## エリアスクリプトの挿入

ST/スクリプト(S)：エリアスクリプト挿入(A) ＋CtrlA

エリアスクリプトは行のすべてをスクリプトで記述します。

1. エリアスクリプトを作成したいセル(行)をクリックします。

2. メニューから【ST/スクリプト(S)】【エリアスクリプト挿入(A)】を選択します。

ラダー編集ウィンドウ内で右クリックメニューから［ST/スクリプト編集(X)］[エリアス別手順クリプト挿入(A)]を選択

![図(元マニュアル p.488)](images/kvstudio/13-01-01.webp)

3 4スクリプト入力後、別手順

ST/スクリプト(S)：ボックススクリプト挿入(B) ＋CtrlB

ボックススクリプトは、いつ、どのタイミングでスクリプトで記述されたプログラムを実行するかをラダーで指定できる、ラダー実行条件付きスクリプトです。

1. ラダーエディタで実行条件を記述し、ボックススクリプトを作成したいセルをクリックします。

2. メニューから【ST/スクリプト(S)】【ボックススクリプト挿入(B)】を選択します。

ラダー編集ウィンドウ内で右クリックメニューから［スクリプト編集(X)］[ボックススク別手順リプト挿入(B)]を選択

3. スクリプトを入力します。

4. スクリプト入力後、 キーを押します。

Esc

- スクリプト記述エリア以外のラダー編集ウィンドウをクリック別手順

- ：キー＋：キーを押す：Ctrl

## 編集方法を切り替える

ST/スクリプト(S)：編集方法切替(N)：直接入力(D)

ウィンドウ(W)

ST/スクリプトの編集方法を選択します。ST/スクリプトエディタ内に直接記述、またはウィンドウを開いて記述するか選択します。

1. メニューから【ST/スクリプト(S)】【編集方法切替(N)】【直接入力(D)】または【ウィンドウ(W)】

を選択します。

直接入力(スクリプトエディタ)

![図(元マニュアル p.489)](images/kvstudio/13-01-02.webp)

ウィンドウ(スクリプト編集ウィンドウ)

![図(元マニュアル p.489)](images/kvstudio/13-01-03.webp)

入力したスクリプトを変換します。

選択したスクリプトのみ変換する

ST/スクリプト(S)：選択ST/スクリプトを変換(S) ＋CtrlT

変換するスクリプトを選択し、メニューから【ST/スクリプト(S)】【選択ST/スクリプトを変換(S)】を選択します。

選択したスクリプトが変換され、変換したスクリプトの行には、ボタンが表示されます。

スクリプトの記述に間違いがある場合は、エラーメッセージが表示され、アウトプットウィンドウにエラー箇所が表示されます。

すべてのスクリプトを変換する

ST/スクリプト(S)：全てのST/スクリプトを変換(C)

メニューから【ST/スクリプト(S)】【全てのST/スクリプトを変換(C)】を選択します。

スクリプトが変換され、スクリプトの行には、 ：ボタンが表示されます。

スクリプトの記述に間違いがある場合は、エラーメッセージが表示され、アウトプットウィンドウにエラー場所が表示されます。

スクリプト変換エラーの警告レベルを設定する

ST/スクリプト(S)：ST/スクリプト警告レベル設定(W)

スクリプト変換時に表示されるエラーの警告レベルを設定します。

ポイント

この機能は、スクリプト警告レベルを設定することで本来表示されるべき警告を表示せず、効率よく変換するためのものです。十分注意してスクリプトを記述し、スクリプトを変換してください。

1. メニューから【ST/スクリプト(S)】【ST/スクリプト警告レベル設定(W)】を選択します。

[ST/スクリプト警告レベル設定]ダイアログボックスが表示されます。

![図(元マニュアル p.490)](images/kvstudio/13-01-04.webp)

「グローバルラベル定義のデータ型と異なるサフィックスを指定したときの警告を出力しない。」のチェックボックスをONにすると、グローバルラベル登録時に定義したデータの型と異なるサフィックスを指定したときに警告を出力しなくなります。

例) グローバルラベル定義で「1ワード符号無し整数：UINT」を指定し、ST/スクリプトで.Lサフィックスを指

定した場合など2警告レベルを選択し、〔OK〕ボタンをクリックします。

警告レベルが設定されます。
