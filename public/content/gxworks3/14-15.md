---
vendor: gxworks3
vendor_label: "GX Works3 (三菱電機)"
id: "14-15"
num: "14.15"
title: "FBプロパティの現在値を初期値に反映する"
category: debug
source: "GX Works3 オペレーティングマニュアル (SH-081214) pp.810-813"
---

# 14.15 FBプロパティの現在値を初期値に反映する

"FBプロパティ管理(オンライン)"画面でシーケンサからFBプロパティの現在値を読み出し、その現在値をプロジェクトのFB

プロパティの初期値に反映できます。

FBプロパティの詳細は、下記を参照してください。

→ FBプロパティの初期値を設定

FBインスタンスの初期値には現在値を反映することができません。

FBインスタンスの初期値に現在値を反映する場合、CSVファイルにエクスポートした現在値を参考に"ラベル初期値設定"画面でFBインスタンスの初期値を設定してください。

→ エクスポートしたCSVファイル

"ラベル初期値設定"画面で初期値を設定する方法は、下記を参照してください。

→ 初期値の設定

## FBプロパティ管理(オンライン)画面

"FBプロパティ管理(オンライン)"画面について説明します。

画面表示

- 選択したFBプロパティの初期値を更新する場合1.下記いずれかのエディタ上で、ファンクションブロックまたはタグFBを選択します。

- ラダーエディタ

- STエディタ

- FBD/LDエディタ

- タグFB設定エディタ2.右クリック ⇒ ショートカットメニュー[FBプロパティ] ⇒ [FBプロパティ管理(オンライン)]を選択します。

![図(元マニュアル p.810)](images/gxworks3/14-15-01.webp)

下記の場合のみ、"FBプロパティ管理(オンライン)"画面を表示できます。

- ラダーエディタ: FBインスタンスを選択する

- STエディタ: トークンにカーソルを合わせる、またはトークン全体を選択する

- FBD/LDエディタ:ファンクションブロック全体を選択する

- すべてのFBプロパティの初期値を管理する場合[オンライン] ⇒ [FBプロパティ管理(オンライン)]を選択します。

または、ツールバーの：(FBプロパティ管理) ⇒ [FBプロパティ管理(オンライン)]を選択します。

![図(元マニュアル p.811)](images/gxworks3/14-15-02.webp)

表示内容

| 項目 | 内容 | 容 | |
|---|---|---|---|
| (1)ツールバー | | 前の相違行 | FBプロパティ一覧内の現在のカーソル位置より前方にある相違行(現在値と初期値が異なる行) |
| | | | にジャンプします。(常時有効) |
| | | 次の相違行 | FBプロパティ一覧内の現在のカーソル位置より後方にある相違行にジャンプします。(常時有効) |
| | | 全選択 | FBプロパティ一覧内の選択可能なすべてのFBプロパティ項目に、チェックを入れます。(常時有効) |
| | | 全解除 | FBプロパティ一覧内の選択可能なすべてのFBプロパティ項目のチェックをはずします。(常時有効) |
| | | 小数点桁指定 | 単精度/倍精度実数型の現在値の小数点桁数を指定します。*1• 指定なし: ―• 指定あり: 1～14相違行フィルタ表示を行っていない場合のみ、桁数を指定できます。小数点桁数指定中に相違行をフィルタ表示した場合、小数点桁数を指定した状態のままフィルタ表示されます。(現在値に対してのみ有効) |
| | 相違 | 違行フィルタ表示/解除 | 相違行のみをフィルタ表示/解除します。フィルタ表示が有効になっている場合、現在値の再読込みが実行されると、フィルタが解除されます。 |

| 項目 | 内容 | | | |
|---|---|---|---|---|
| (2)FBインスタンス名 | 選択したファンクションブロックプロジェクトに登録されているすされます。 | クまたはタグFBのFBインすべてのファンクション | ンスンブ | スタンス名が表示されます。ブロックまたはタグFBの現在値を読み出している場合、"すべて"が表示 |
| (3) FBプロパティ一覧 | "FBプロパティ管理(オンライン)"[オンライン] ⇒ [FBプロパティ管理クションブロックおよびタグFBの• グローバルラベルに設定されて• プログラムのローカルラベルに | 画面の表示時に、選択理(オンライン)]を選択しのFBプロパティが表示さているFBインスタンスに設定されているFBイン | ししてされンス | たFBのFBプロパティが一覧で表示されます。て"FBプロパティ管理(オンライン)"画面を表示した場合、下記のファンれます。スタンス |
| | 更新選択 | [初期値更新の実行]ボします。• チェックあり: 現在• チェックなし: 現在チェックの状態は、F | ボタ値値Bイ | ンをクリックしたときに、現在値を初期値へ反映するかどうかを選択を初期値へ反映するを初期値へ反映しない*2インスタンス単位でプロジェクトへ保存されます。 |
| | 階層 | 表示対象のファンクシ各ファンクションブロ• ファンクションブロ• ファンクションブロ• タグデータ: タグデ | ショロッロッロッー | ョンブロックおよびタグデータの階層が表示されます。ックの表示内容を示します。ック(ローカルラベル定義): プログラムのPOU名/FBラベル名ック(グローバルラベル定義)、タグFB: FBラベル名タのラベル名 |
| | ラベル名 | 対象ファンクションブ | ブロ | ロック内のFBプロパティ項目名が表示されます。 |
| | 現在値 | シーケンサから取得し現在値と初期値が異な | したなる | たFBプロパティの現在値が表示されます。る場合、文字色が赤色で表示されます。*3 |
| | 初期値 | プロジェクトから取得現在値と初期値が異な | 得しなる | したFBプロパティの初期値が表示されます。る場合、文字色が赤色で表示されます。*3 |
| (4)行ヘッダ | 現在値を初期値へ反映できない場 | 場合、エラーアイコン( | | )が表示されます。 |
| (5)説明欄 | FBプロパティ一覧で選択しているラベルコメントを表示するには、表示対象に設定します。(→ 91 | るラベルのデータ型、お"複数コメント表示設ページ コメントの表示 | およ定と | よびラベルコメントが表示されます。画面*4で、説明欄に表示させるラベルコメントのコメントタイトルを読出し) |
| (6)[ファイルへエクスポート]ボタン | FBプロパティ一覧に表示されてい | いるFBプロパティの現在 | 在値 | 値と初期値をCSVファイルへエクスポートします。 |
| (7)[現在値の再読込み]ボタン | シーケンサからFBプロパティの現 | 現在値を再度読み出しま | ます | す。 |
| (8)[初期値更新の実行]ボタン | 読み出したFBプロパティの現在値 | 値を初期値に反映します | す。 | |

*1：7桁以上の場合は倍精度実数型の項目にのみ反映され、単精度実数型の項目は6桁で表示されます。*2："ラベル初期値設定"画面でFBインスタンスの初期値を設定している場合、チェックなしで固定されます。*3：FBプロパティ一覧の現在値と初期値は、バイナリで比較されます。(例: 1.0と1.00は同じと判断されます。)*4：下記のメニューから、"複数コメント表示設定"画面を表示できます。[表示] ⇒ [複数コメント] ⇒ [表示設定]

初期値の更新を実行すると、初期値が更新されたラベルに関係するプログラムは未変換状態になります。

### フォーマット

エクスポートしたCSVファイルのフォーマットの詳細は、下記を参照してください。

→ エクスポートしたCSVファイル
